// document.addEventListener('DOMContentLoaded', () => {
//     window.Router.initRouter();
  
//     document.getElementById('page_list').addEventListener('click', (e) => {
//       const target = e.target;
//       if (target.tagName === 'A' && target.dataset.id) {
//         e.preventDefault();
//         window.Router.navigateTo(target.dataset.id);
//       }
//     });
//   });